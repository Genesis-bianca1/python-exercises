if __name__ == "__main__":
  # A program to calculate the volume of a cuboid.
  width = (float(input('Enter the width with one decimal place: ')))
  height = (float(input('Enter the height with one decimal place: ')))
  depth = (float(input('Enter the depth with one decimal place: ')))
  cuboid = (width * height * depth)
  print('The volume of your cuboid is: ', cuboid)